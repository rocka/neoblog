aside
    p #{side.title}
    each group in side.items
        ul
            each item in group
                li
                    if item.link && item.name
                        a(href=item.link) #{item.name}
                    else if item.text
                        span #{item.text}
        div(class="line")
