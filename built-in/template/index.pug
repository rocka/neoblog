extends layout

block content
    h1 #{indexHeading}
    each article in articles
        article
            h1
                a(class="title" href=`/article/${article.file.base}`)  #{article.meta.title}
            != article.excerpt
